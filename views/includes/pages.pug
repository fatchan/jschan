| Page: 
- for(let i = 1; i <= maxPage; i++)
	if page === i
		a.bold(href=`/${board._id}/${i === 1 ? 'index' : i}.html`) [#{i}]
	else
		a(href=`/${board._id}/${i === 1 ? 'index' : i}.html`) #{i}
	|  
|   |  
